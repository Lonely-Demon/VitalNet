fastapi==0.139.0
uvicorn[standard]==0.50.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
groq==1.5.0
google-generativeai==0.8.6
# scikit-learn is pinned EXACTLY (not >=) because trained .pkl models are
# only guaranteed to unpickle with the scikit-learn version that trained
# them — a newer minor version reorganised internal modules (e.g. `_loss`)
# and broke unpickling of models trained on older releases. If you bump
# this version, you MUST re-run scripts/train_classifier.py and commit the
# regenerated triage_classifier.pkl + triage_classifier.onnx in the same
# change, or the app will crash at startup (load_classifier() raises).
scikit-learn==1.9.0
# shap==0.51.0 — do not downgrade to 0.46.0 (source build fails on Windows+Python 3.13)
# do not upgrade past 0.51.x without re-verifying SHAP round-trip on triage_classifier.pkl
shap==0.51.0
# numpy is held below 2.4 for now — 2.5.x requires Python >=3.12, which
# would break local dev on the documented-supported Python 3.11 floor (see
# README.md's "3.11+ also works for local development"). Revisit once this
# project's local-dev floor moves to 3.12+, ideally alongside a retrain to
# re-verify the pkl round-trips cleanly under the new numpy.
numpy==2.2.6
# httpx is capped below 0.28 because supabase==2.10.0 requires httpx>=0.26,<0.28 —
# bumping past that needs a supabase-py upgrade too (a bigger, separately-risky
# change — postgrest/gotrue/realtime all move together), not a solo httpx bump.
httpx==0.27.2
python-jose[cryptography]==3.5.0
supabase==2.10.0
email-validator==2.3.0
slowapi==0.1.10
json-repair==0.61.2
python-json-logger==4.1.0
pywebpush==2.3.0
# Required by FastAPI/Starlette to parse multipart/form-data — used by
# POST /api/voice/transcribe's audio file upload (app/services/voice.py).
python-multipart==0.0.32
prometheus_client==0.25.0
