# Red Team Reports

Each specialist writes one markdown report here.

Use the agent name as the filename, for example:
- `reports/red-team/security-auth-specialist.md`
- `reports/red-team/ux-mobile-specialist.md`

Rules:
- overwrite the file on each run
- include exact evidence, line refs, impact, and fix
- treat the file as the canonical specialist handoff
